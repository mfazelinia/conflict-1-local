export class Team {
  name: string;
  color: string;
  textColor: string;

  constructor(name: string, color: string, textColor: string) {
    this.name = name;
    this.color = color;
    this.textColor = textColor;
  }
}
